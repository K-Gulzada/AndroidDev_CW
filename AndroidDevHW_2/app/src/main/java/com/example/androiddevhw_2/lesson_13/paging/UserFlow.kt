package com.example.androiddevhw_2.lesson_13.paging

import androidx.paging.Pager
import androidx.paging.PagingConfig
import androidx.paging.cachedIn
import java.lang.Exception

/*
object UserFlow {
    val flow = Pager(PagingConfig(pageSize = 10)){
        UsersPagingSource(throw Exception())
    }.flow.cachedIn() // создать фрагмент, вьюмодель. запихнуть сюда вьюмодель. это не точно
    // добить

}*/
